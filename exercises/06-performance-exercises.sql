-- Exercise 06: Performance and Query Plans
-- Write your answers below each question.

-- 1. Use EXPLAIN to inspect a query that filters superstore by region = 'West'.

-- 2. Use EXPLAIN ANALYZE to inspect a query that aggregates sales by sub_category.

-- 3. Create an index on superstore(region), then rerun the filter plan from question 1.

-- 4. Use EXPLAIN on a join between superstore and periode grouped by year_actual.

-- 5. Use EXPLAIN ANALYZE on a query that returns the top 5 customers by total profit.

-- 6. Create an index on superstore(customer_id), then rerun the top-customer plan
--    and compare whether the plan changes.
