-- Exercise 10: Subqueries and EXISTS
-- Write your answers below each question.

-- 1. Write a scalar subquery that shows each row's sales and the overall average sales.

-- 2. Return orders where sales is greater than the overall average sales.

-- 3. Use a correlated subquery to return rows where profit is above the average profit for the same sub_category.

-- 4. Use EXISTS to find customers who have at least one order with discount greater than 0.4.

-- 5. Use NOT EXISTS to find product_name values that never had a negative profit row.

-- 6. Build a derived-table subquery that returns region totals ordered by total_sales descending.
